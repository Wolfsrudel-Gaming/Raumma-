{
  "name": "raumwerk-native",
  "version": "0.1.0",
  "private": true,
  "description": "Native Hüllen (Android + iOS) für die RAUMWERK-Web-App – via Capacitor.",
  "scripts": {
    "web:copy": "node scripts/kopiere-web.mjs",
    "sync": "npm run web:copy && cap sync",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "android": "npm run sync && cap open android",
    "ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
